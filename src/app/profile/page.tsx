export default function ProfilePage() {
  // This page is part of a profile section that is not currently used by guests.
  // Admin functionalities are managed elsewhere.
  return (
    <div>
      <h1>Profile</h1>
      <p>This section is not active for guest users.</p>
    </div>
  );
}
